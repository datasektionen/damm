{
  "name": "damm",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "start": "node server/app.js",
    "test:install": "cd server && npm install",
    "test": "cd server && npm test",
    "dev:server": "nodemon server/app.js",
    "dev:client": "cd client && npm start",
    "postinstall": "cd server && npm install && cd ../client && npm install"
  }
}
